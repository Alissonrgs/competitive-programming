#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    ll x, m;
    
    while( scanf( "%Ld%Ld", &x, &m ) && x ) {
        printf( "%Ld\n", x*m );
    }

    return 0;
}
